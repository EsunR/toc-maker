{
  "name": "toc-maker",
  "version": "1.2.1",
  "description": "由文章HTML文本动态生成文章目录",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup ./src/index.js --file ./dist/bundle.js --format cjs"
  },
  "author": "EsunR",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "rollup": "^1.26.0"
  }
}
